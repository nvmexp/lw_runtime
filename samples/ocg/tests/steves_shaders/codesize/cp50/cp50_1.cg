// cp50_1.cg - Simple compute program.

float memory[20] : global;

void main(uniform float4 gcolor : GCOLOR)
{
    float4 color;

    color = gcolor;
    memory[0] = color.x;
    memory[1] = color.y;
    memory[2] = color.z;
    memory[3] = color.w;
} // main

