include $(LWVS_DIR)/libs/yaml-0.3.0/yaml.lwmk

#Only needed by the EUD plugin, which is inactive
#include $(LWVS_DIR)libs/libtar-1.2.11/tar.lwmk

#Don't force yaml's object files into every project that includes this makefile
#LOCAL_OBJECTS += $(yaml_TARGET)

libs_TARGET := $(yaml_TARGET) 
